<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data>
        <!-- Customize header -->
        <template id="custom_header" inherit_id="website.layout" name="Custom Header">
            <!--Assign an id -->
            <xpath expr="//div[@id='wrapwrap']/header" position="attributes">
                <attribute name="id">l_header</attribute>
            </xpath>
            <xpath expr="//div[@id='wrapwrap']/header/div" position="before">
                <div class="container">
                    <div class="row">
                        <strong>Добро пожаловать</strong> на наш сайт!
                    </div>
                </div>
            </xpath>
        </template>
        <template id="custom_logo" inherit_id="website.layout_logo_show" name="Custom Logo">
            <xpath expr=".//a[@class='navbar-brand logo']" position="replace">
                <a href="/" class="navbar-brand logo">
                    <img src="/logo.png" t-att-alt="'Логотип %s' % res_company.name" t-att-title="res_company.name"/>
                </a>
            </xpath>
        </template>

    </data>
</odoo>